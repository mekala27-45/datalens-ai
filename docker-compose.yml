services:
  datalens:
    build: .
    ports:
      - "8501:7860"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - DATALENS_DEMO_MODE=${DATALENS_DEMO_MODE:-true}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
